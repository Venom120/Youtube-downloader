# Backend Server URLs
# For local development use your machine IP (not localhost)
# Example: http://192.168.1.100:8000
BACKEND_URL=http://localhost:8000
# Keep BACKEND_URL as base host only (no trailing /api)
WS_URL=ws://localhost:8000
# Keep WS_URL as base host only (no trailing /ws)

# App Identifier (must match backend ALLOWED_APP_ID)
APP_ID=com.venom120.ytdownloader

# Download Paths
# Cache directory for temporary downloads (will be set by app at runtime)
# CACHE_PATH will be auto-configured

# Final download destination (will be set by user preference)
# DOWNLOAD_PATH will be auto-configured

